@model IEnumerable<CoffeeBean.Entity.Product>
@{
    ViewData["Title"] = "Authentification";
}

<h1 class="bg-info text-white">Index</h1>
<h2>Hello @User!.Identity.Name</h2>

@if (User!.IsInRole("Admin"))
{
}

@if (User!.IsInRole("User"))
{
}
@if (!User?.Identity?.IsAuthenticated ?? false)
{
    <p>Категории</p>
    <div>
        @foreach (var category in ViewBag.Category)
        {
        <a class="btn btn-sm btn-primary" asp-controller="ProductsReview" asp-action="Index" asp-route-id="@category.Value">@category.Text</a>
        }
    </div>
}
